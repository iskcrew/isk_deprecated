<div class="schedule_info" id="schedule_<%=schedule.id%>" >
	<div class="schedule_header">
		<%=link_to schedule.name, schedule_path(schedule)%>
	</div>
	<div class="schedule_data">
		<ul>
			<li>Events in this schedule: <%=schedule.schedule_events.count%></li>
			<li>Group for generated slides: <%=link_to schedule.slidegroup.name, group_path(schedule.slidegroup)%></li>
			<li>Maximum number of generated slides: <%schedule.max_slides == -1 ? 'Unlimited' : schedule.max_slides%></li>
			<li>Generate up next -slides? <%=schedule.up_next ? 'Yes': 'No'%></li>
			<li>Group for up next -slides: <%=link_to schedule.up_next_group.name, group_path(schedule.up_next_group)%></li>
		</ul>
	</div>
	<div class="schedule_actions">
		<%=link_to 'Details', schedule_path(schedule), :class => 'button'%>
		<%=link_to 'Edit', edit_schedule_path(schedule), :class => 'button'%>
	</div>
</div>