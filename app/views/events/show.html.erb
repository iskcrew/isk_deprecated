<h1>Event information for: <%=@event.name%></h1>
<p>Current ISK event? <%=@event.current ? 'Yes' : 'No'%></p>
<p>Group for ungrouped slides: <%=link_to @event.ungrouped.name, group_path(@event.ungrouped)%></p>
<p>Group for thrashed slides: <%=link_to @event.thrashed.name, group_path(@event.thrashed)%></p>