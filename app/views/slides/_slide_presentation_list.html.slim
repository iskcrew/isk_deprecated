/ Render info about any presentations this slide belongs to
/ Also show all associated displays for each presentation

div.slide_presentation_list.info_block#presentations
	h2 Presentations
	- if @slide.presentations.empty?
		p This slide doesn't belong to any presentations.
	- else
		p This slide belongs to the following presentations:
		ul
			- @slide.presentations.each do |p|
				li
					=> link_to p.name, presentation_path(p)
					- unless p.displays.empty?
						| running on displays:
						ul
							- p.displays.each do |d|
								li =link_to d.name, display_path(d)
