<script>
var dispatcher = new WebSocketRails('<%=root_url.to_s.split("//")[1]%>websocket');
slidelist = dispatcher.subscribe('slidelist');
slidelist.bind('updated_slide', function(data) {$.ajax({
	type: "GET",
	url: "<%=url_for :controller => :slides%>/" + data,
	dataType: 'script'
});})
</script>